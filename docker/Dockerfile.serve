FROM python:3.11-slim
WORKDIR /app
COPY pyproject.toml README.md /app/
COPY src /app/src
RUN pip install --no-cache-dir -e ".[serve]"
ENV LLM_OPS_FAKE_MODEL=1
CMD ["uvicorn", "llm_customization_ops.serving.app:app", "--host", "0.0.0.0", "--port", "8000"]
